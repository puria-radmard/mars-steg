from setuptools import setup, find_packages

setup(
    name="mars-steg",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your project dependencies here
        # You can copy these from requirements.txt
    ],
    author="mars-steg",
    description="Mars Steg project",
    python_requires=">=3.6",
)